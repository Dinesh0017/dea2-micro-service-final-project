spring:
  application:
    name: order-management-service
  datasource:
    url: jdbc:mysql://localhost:3306/order_management_service
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
